{% spaceless %}
{% block easy_shop_address_render %}
    <div class="row">
        <div class="col-sm-{% if showEdit %}8{% else %}12{% endif %}">
            <address>
                {% block easy_shop_address_name %}
                    {% if showName %}<strong>{{ address.name }}</strong><br/>{% endif %}
                {% endblock %}
                {% block easy_shop_address_address %}
                    {{ address.address|raw }}
                {% endblock %}
            </address>
        </div>
        {% block easy_shop_address_edit %}
            {% if showEdit %}
                <div class="col-sm-3">
                    <a class="btn btn-primary btn-xs pull-right" href="{{ url('easy_shop_customer_address_edit', {'id' : address.id, 'context': context}) }}"><span class="glyphicon glyphicon-pencil">&nbsp;</span>{{ 'easy_shop_customer_address_edit'|trans({}, "SonataCustomerBundle") }}</a>
                </div>
            {% endif %}
        {% endblock %}
    </div>
{% endblock %}
{% endspaceless %}
