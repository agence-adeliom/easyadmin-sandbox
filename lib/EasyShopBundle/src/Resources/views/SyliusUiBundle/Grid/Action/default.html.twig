{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}

{% set path = options.link.url|default(sylius_route(options.link.route, options.link.parameters|default({}))) %}

{{ buttons.default(path, action.label, null, action.icon) }}
