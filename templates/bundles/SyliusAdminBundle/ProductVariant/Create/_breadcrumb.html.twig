{% import '@SyliusAdmin/Macro/breadcrumb.html.twig' as breadcrumb %}

{% set breadcrumbs = [
        { label: 'sylius.ui.administration'|trans, url: sylius_route('sylius_admin_dashboard') },
        { label: 'sylius.ui.products'|trans, url: sylius_route('sylius_admin_product_index') },
        { label: resource.product.name|default(resource.product.code), url: sylius_route('sylius_admin_product_update', {'id': resource.product.id}) },
        { label: 'sylius.ui.variants'|trans, url: sylius_route(configuration.getRouteName('index'), {'productId': resource.product.id}) },
        { label: 'sylius.ui.new'|trans }
    ]
%}

{{ breadcrumb.crumble(breadcrumbs) }}
