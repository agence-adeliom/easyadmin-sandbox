{% for name, gateway in gatewayFactories %}
    <a href="{{ sylius_route('sylius_admin_payment_method_create', { 'factory': name }) }}" id="{{ name }}" class="item">
        {{ gateway|trans }}
    </a>
{% endfor %}
